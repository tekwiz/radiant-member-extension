%h1 Import members from CSV
.form
  - form_for :member, :url => import_from_csv_path, :html => {:multipart => true} do
    %table
      %tr
        %td
          %label{:for => 'file_csv'} Choose a CSV file:
          %p.note The first line of the CSV file must be the headers, and they must be in the order Name, Email.
      %tr
        %td
          = file_field 'file', 'csv'
      %tr
        %td
          = submit_tag 'Import data'
          or
          = link_to 'Cancel', members_path
          
%h4 CSV File Headers:
:preserve
  <pre>nl_mailed, list_serv_removal, first_name, last_name, street, city, state, zip, paid, notes, dob, lettered, home, work, cell, fax, email, email_2, occupation, spouse</pre>
